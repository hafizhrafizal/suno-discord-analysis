# Core web framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# Data processing
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<3.0.0

# Vector databases — install the one(s) you need based on VECTOR_DB in .env
qdrant-client>=1.9.0      # required for VECTOR_DB=qdrant
chromadb==1.5.7         # required for VECTOR_DB=chroma_persistent or chroma_http

# OpenAI client
openai>=1.40.0,<2.0.0

# Template / static serving
jinja2>=3.1.0,<4.0.0
python-multipart>=0.0.9,<1.0.0

# Async HTTP client (Google OAuth token exchange)
httpx>=0.27.0,<1.0.0

# Load .env files for local development
python-dotenv>=1.0.0,<2.0.0
